<Window x:Class="WarehouseAutomatisaion.Desktop.Wpf.ProductPriceUpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Обновить цены"
        Width="520"
        Height="360"
        MinWidth="500"
        MinHeight="330"
        WindowStartupLocation="CenterOwner"
        Background="#F7F9FD">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,18">
            <TextBlock Text="Обновить цены"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Foreground="#17213A" />
            <TextBlock x:Name="ProductsCountText"
                       Margin="0,6,0,0"
                       FontSize="13"
                       Foreground="#6E7B98" />
        </StackPanel>

        <Border Grid.Row="1"
                Background="#FFFFFF"
                BorderBrush="#E4EAF5"
                BorderThickness="1"
                CornerRadius="14"
                Padding="18">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Text="Вид цены" Foreground="#6E7B98" FontSize="12" Margin="0,0,0,5" />
                <ComboBox x:Name="PriceTypeComboBox" Grid.Row="1" Height="36" Margin="0,0,0,14" />

                <TextBlock Grid.Row="2" Text="Причина изменения" Foreground="#6E7B98" FontSize="12" Margin="0,0,0,5" />
                <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,14">
                    <ComboBox x:Name="ModeComboBox" Width="220" Height="36" Margin="0,0,12,0" />
                    <TextBox x:Name="ValueTextBox" Width="160" Height="36" VerticalContentAlignment="Center" />
                </StackPanel>

                <CheckBox x:Name="ApplyImmediatelyCheckBox"
                          Grid.Row="4"
                          Content="Сразу провести документ и обновить текущую цену"
                          IsChecked="True"
                          FontSize="13"
                          Foreground="#34415F" />
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,18,0,0">
            <TextBlock x:Name="ValidationText"
                       Foreground="#D92D20"
                       VerticalAlignment="Center" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена"
                        Width="120"
                        Height="38"
                        Margin="0,0,10,0"
                        Click="HandleCancelClick" />
                <Button Content="Сохранить цены"
                        Width="160"
                        Height="38"
                        Background="#4F5BFF"
                        BorderBrush="#4F5BFF"
                        Foreground="#FFFFFF"
                        FontWeight="SemiBold"
                        Click="HandleSaveClick" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
