<UserControl x:Class="WarehouseAutomatisaion.Desktop.Wpf.SectionOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Background="#F7F9FD">
    <UserControl.Resources>
        <Style x:Key="SectionGroupTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14.5" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#1F8F50" />
            <Setter Property="Margin" Value="0,0,0,12" />
        </Style>

        <Style x:Key="SectionCommandButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="2,3" />
            <Setter Property="Margin" Value="0,0,0,2" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionCommandTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#4F5BFF" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsImplemented}" Value="False">
                    <Setter Property="Foreground" Value="#5F6F95" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="36,18,36,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top toolbar: search + settings + close (как в 1С header панели разделов) -->
        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="1"
                    Width="320"
                    Height="32"
                    Background="#FFFFFF"
                    BorderBrush="#D7DEEC"
                    BorderThickness="1"
                    CornerRadius="6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="32" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="SearchBox"
                             Grid.Column="0"
                             BorderThickness="0"
                             Background="Transparent"
                             VerticalContentAlignment="Center"
                             Padding="10,0"
                             FontSize="12.5"
                             TextChanged="HandleSearchTextChanged" />
                    <TextBlock x:Name="SearchPlaceholderText"
                               Grid.Column="0"
                               Text="Search (Ctrl+F)"
                               IsHitTestVisible="False"
                               Foreground="#9AA4BC"
                               VerticalAlignment="Center"
                               Margin="12,0" />
                    <TextBlock Grid.Column="1"
                               FontFamily="Segoe Fluent Icons"
                               Text="&#xE721;"
                               Foreground="#9AA4BC"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               FontSize="13" />
                </Grid>
            </Border>

            <Button Grid.Column="2"
                    Width="32"
                    Height="32"
                    Margin="8,0,0,0"
                    Background="Transparent"
                    BorderBrush="#D7DEEC"
                    BorderThickness="1"
                    Cursor="Hand"
                    ToolTip="Настройки раздела"
                    Click="HandleSettingsClick">
                <TextBlock FontFamily="Segoe Fluent Icons"
                           Text="&#xE713;"
                           FontSize="13"
                           Foreground="#5F6F95" />
            </Button>

            <Button Grid.Column="3"
                    Width="32"
                    Height="32"
                    Margin="6,0,0,0"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    ToolTip="Закрыть вкладку"
                    Click="HandleCloseClick">
                <TextBlock FontFamily="Segoe Fluent Icons"
                           Text="&#xE894;"
                           FontSize="13"
                           Foreground="#5F6F95" />
            </Button>
        </Grid>

        <!-- Content: 3 колонки групп -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Padding="0,4,0,16">
            <ItemsControl x:Name="GroupsItemsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel x:Name="GroupPanel"
                                    Width="320"
                                    Margin="0,0,40,28">
                            <TextBlock Text="{Binding Title}"
                                       Style="{StaticResource SectionGroupTitleStyle}" />
                            <ItemsControl ItemsSource="{Binding Items}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Tag="{Binding}"
                                                Style="{StaticResource SectionCommandButtonStyle}"
                                                Click="HandleCommandClick">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock FontFamily="Segoe Fluent Icons"
                                                           Text="&#xE735;"
                                                           Foreground="#FF9F1A"
                                                           FontSize="11"
                                                           Width="16"
                                                           VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsImplemented}" Value="False">
                                                                    <Setter Property="Visibility" Value="Hidden" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <TextBlock Text="{Binding Caption}"
                                                           Margin="8,0,0,0"
                                                           Style="{StaticResource SectionCommandTextStyle}" />
                                            </StackPanel>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
